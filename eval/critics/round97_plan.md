# Round 97 plan — STRATEGY A4: definitional-paragraph-opener scrub on best-on-A3 base (single-axis)

## Strategy

Per fresh dispatch STRATEGY A4: rewrite every paragraph that opens with "X is Y" or "This section examines" / "This dissertation argues" / "The following section..." into a concrete observation or specific case opener. Keep all other prose intact.

## Branch

If R96 ≥ 2/3: branch from R96.
Else if any prior A_X ≥ 2/3, branch from that best-on-axis.
Else branch from R80 baseline.

## Implementation

a. Identify every paragraph opener that fits the definitional pattern (subject + copula = noun-phrase, or "This section/chapter/dissertation X").
b. For each, rewrite as a concrete-particular opener: a date, a specific numeric, a primary-source quote, an observed agent action, a specific data-trace.
c. Preserve openers that are already concrete (date-led, numeric-led, named-action-led).

## Pareto rule

Same as prior. ≥ 2/3 = save as BEST_AXIS_A4. ≥ 3/3 = SHIP. Else revert to best-on-axis baseline and pivot to STRATEGY B.
